from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression

def train_model(X, y):
  """
  Train a linear regression model on the recipe data and return the trained model.
  """
  return X

def predict_difficulty(model, X):
  """
  Use the trained model to predict the difficulty of a recipe given its features.
  """
  return model
